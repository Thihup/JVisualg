algoritmo "fim algoritmo"

procedimento proc(x: inteiro)
inicio

    se x < 2 entao
        fimalgoritmo
    fimse

    escreva("Apareci")

fimprocedimento

inicio

    proc(2)
    proc(1)

    escreval("Nao devo aparecer")


fimalgoritmo
